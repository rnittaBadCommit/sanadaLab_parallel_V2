#ifndef SERVER_HPP
# define SERVER_HPP

namespace rnitta
{


class Server
{
	public:
		// constructor
		Server();
		Server( const Server& other );
		~Server();
		Server&	operator=( const Server& other );

	private:
	
};	// class Server


};	// namespace rnitta

#endif
