#ifndef VERSIONUPDATER_HPP
# define VERSIONUPDATER_HPP

namespace rnitta
{


class VersionUpdater
{
	public:
		// constructor
		VersionUpdater();
		VersionUpdater( const VersionUpdater& other );
		~VersionUpdater();
		VersionUpdater&	operator=( const VersionUpdater& other );
	
	private:

};	// class VersionUpdater


};	// namespace rnitta

#endif
